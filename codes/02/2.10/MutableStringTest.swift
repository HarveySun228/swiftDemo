var mutableStr = "swift"// var声明的String是可变的mutableStr += " is a good programming language"println(mutableStr) // 输出 swift is a good programming language"// 清空mutableStr的所有字符mutableStr.removeAll(keepCapacity:false)println(mutableStr) // 输出空let immutableStr = "Objective-C"// let声明的String是不可变的，因此不能追加//immutableStr += " is a classic programming language"// 不可变字符串不允许改变字符串中字符序列，因此下面代码编译出错//immutableStr.removeAll(keepCapacity:false)