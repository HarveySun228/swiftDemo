import Foundationvar name = "fkit.org"var myBook:String = "疯狂软件教育"// 调用构造器来创建字符串var str2 = String()  // 创建空字符串// 创建多个重复字符的字符串var str3 = String(count:5, repeatedValue: Character("f"))println(str3)// 判断指定字符串是否为空println(str2.isEmpty)let prefix = "fkit.org"// 使用+拼接字符串var info = prefix + " is a good training center"println(info)  // 输出 fkit.org is a good training center// 程序定义了一个NSString类型的变量，可以直接将Swift的字符串赋值给s变量var s: NSString = "crazyit.org"// 调用NSString的substringWithRange方法var subStr = s.substringWithRange(NSMakeRange(2, 5))println(subStr) //输出azyit