var bookPrice : Int8 = 79var itemPrice : Int16 = 120// bookPrice是Int8类型，但变量a是Int16类型，因此下面代码错误//var a : UInt16 = bookPrice// bookPrice与itemPrice类型不同，不能进行加法运算//var total = bookPrice + itemPrice// 显式将bookPrice强转为Int16类型var a : Int16 = Int16(bookPrice)  // ①// 显式将bookPrice强转为Int16类型，整个表达式结果为Int16类型，// 故total的类型为Int16，计算出来的结果（199）位于Int16的取值范围之内，因此程序正确var total = Int16(bookPrice) + itemPrice  // ②println("total的值为:\(total)")// 显式将itemPrice强转为Int8类型，整个表达式结果为Int8类型，// 故tot的类型为Int9，计算出来的结果（199）超过了Int8的取值范围之内，因此程序运行出错var tot = bookPrice + Int8(itemPrice)  // ③println(tot)