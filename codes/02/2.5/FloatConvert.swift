var width : Float = 2.3;var height : Double = 4.5;// width必须显式强转为Double之后，才能赋值给变量a。var a: Double = Double(width)println("a的值为:\(a)")// 将height强转为Float之后再进行计算，整个表达式的类型都是Float类型，// 因此area1的类型也被推断为Float类型var area1 = width * Float(height)// 将width强转为Double之后再进行计算，整个表达式的类型都是Double类型，// 因此area2的类型也被推断为Double类型var area2 = Double(width) * heightlet multi : Int = 5// 将multi强转为Double后进行计算，整个表达式的类型都是Double类型，// 因此totalHeight1的类型也被推断为Double类型var totalHeight1 = height * Double(multi)  // ①// 将height强转为Int后进行计算，整个表达式的类型都是height类型，// 因此totalHeight2的类型也被推断为Int类型var totalHeight2 = Int(height) * multi  // ②