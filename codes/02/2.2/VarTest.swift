// 声明变量时显式指定类型var b : Int// 声明变量时指定初始值，编译器会根据初始值确定该变量的类型为Stringvar name = "crazyit.org"b = 20; // b的类型是Int（整型），赋值正确name = "fkit.org" // name类型为String，赋值正确//name = 12 // name类型为String，但12为Int，赋值失败// 声明变量时既显式指定类型，也指定初始值，// 显式指定的类型与初始值的类型一致，声明变量正确var age: Int = 25;age = 12; // age的类型是Int，赋值正确// 声明变量时既显式指定类型，也指定初始值，// 显式指定的类型与初始值的类型不一致，声明变量失败//var sun : String = 500;// 同时声明多个变量，而且不需要变量具有相同的类型var a = 20 , d:String , c = "swift"// 输出变量name的值println(name)