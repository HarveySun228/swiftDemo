// 定义常量，没有显式指定类型，编译器根据初始值确定常量的类型let maxAge = 120// 定义常量时，既显式指定常量的类型，也指定常量的初始值let eduName : String = "疯狂软件教育"// 常量不允许重新赋值，因此下面代码是错误的//maxAge = 12// 同时定义多个变量let myName = "fkit" , myAge = 8println("myName的值为:\(myName) , myAge的值为:\(myAge)")