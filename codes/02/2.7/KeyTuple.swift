// 定义元组变量，并指定初始值，// 系统推断该元组类型为(height:Int, 体重:Int, status:String)var health = (height:182 , 体重:78 , status:"良好")// 使用元组类型来定义元组变量var score : (swift:Int , oc:Int , lua:String , ruby:Double)// 简单为每个元素指定值，此时必须按顺序为每个元素指定值score = (98 , 89 , "及格" , 20.4)  // ①// 通过key为元组的元素指定值，在这种方式下，元组内各元素的顺序可以调换score = (oc:89 , swift:98 , lua:"及格" , ruby:4.4)  // ②println("health的身高为：\(health.height)")println("health的体重为：\(health.体重)")println("Swift成绩：\(score.swift),OC成绩：\(score.oc),"	+ "Lua成绩：\(score.lua),Ruby成绩：\(score.ruby)")