// 定义元组变量，并指定初始值，系统推断该元组类型为(Int, Int, String)var health = (182 , 78 , "良好")// 使用元组类型来定义元组变量var score : (Int , Int , String , Double)// 为元组变量赋值时必须为所有成员都指定值score = (98 , 89 , "及格" , 20.4)// 元组的成员又是元组var test : (Int , (Int , String))test = (20 , (15 , "Swift"))// 输出元组println("health元组的值为：\(health)")println("score元组的值为：\(score)")println("test元组的值为：\(test)")// 根据索引访问元组的元素println("health元组的身高元素为：\(health.0)")println("health元组的体重元素为：\(health.1)")println("score元组的Swift为：\(score.0)")println("score元组的Objective-C为：\(score.2)")println("test元组的第2个元素的第1个元素为：\(test.1.0)")// 将元组拆分成多个变量var (height ,weight, status) = healthprintln("身高:\(height)、体重:\(weight)、身体状态:\(status)")// 将元组拆分成多个常量， 忽略元组的第四个元素let (swift, oc , lua, _) = scoreprintln("Swift成绩:\(swift)、OC成绩:\(oc)、Lua成绩:\(lua)")