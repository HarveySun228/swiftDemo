// 下面代码是正确的var a:Int = 56// 下面代码需要隐式地将2999999999转为Int32使用，因此编译器将会报错//var bigValue : Int32 = 2999999999;// 下面代码是正确的var bigValue2: Int64 = 2999999999;//println(bigValue);println(bigValue2);println(Int16.min)println(Int16.max)// 无符号整数的最小值为0，最大值比对应Int16的最大值大一倍println(UInt16.min)println(UInt16.max)var binValue = 0b10101110// 以0O开头的整数数值是8进制的整数var octalValue = 0o13// 以0x开头的整数数值是16进制的整数var hexValue1 = 0x13var hexValue2 = 0xaFprintln("binValue的值为：\(binValue)")println("octalValue的值为：\(octalValue)")println("hexValue1的值为：\(hexValue1)")println("hexValue2的值为：\(hexValue2)")let oneMillion = 1_000_000let price = 0079 // price实际的值为79let android = 0109 // android实际的值为109