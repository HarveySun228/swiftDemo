// 定义一个二维数组var a : [[Int]]// 把a当成一维数组进行初始化，初始化a是一个长度为4的数组// a数组的数组元素又是引用类型a = Array<Array<Int>>(count:4 , repeatedValue:[])// 把a数组当成一维数组，遍历a数组的每个数组元素for var i = 0 , len = a.count; i < len ; i++{	println(a[i]);}// 初始化a数组的第一个元素a[0] = Array<Int>(count: 2, repeatedValue:0)// 访问a数组的第一个元素（又是数组）的第二个元素a[0][1] = 6;// a数组的第一个元素是一个一维数组，遍历这个一维数组for var i = 0 , len = a[0].count ; i < len ; i++{	println(a[0][i]);}