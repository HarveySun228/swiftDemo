// 定义一个User类，类是引用类型class User {	var name: String	init(name:String)	{		self.name = name	}}var users = [User(name:"孙悟空"), User(name:"猪八戒") , User(name:"唐僧")]// 将users赋值给cp1、cp2实际上会先复制数组的2个副本，// 然后分别将2个副本赋值给cp1、 cp2var cp1 = users;var cp2 = users;// 由于users数组的元素是User对象，也就是引用类型，// 因此程序不会复制数组中各应用所引用的User对象。// 因此程序对cp1[0]、 cp2[0]的修改对相互之间会产生任何影响cp1[0].name += "齐天大圣"cp2[0].name += "弼马温"println(users[0].name)println(cp1[0].name)println(cp2[0].name)