import Foundation// 输出trueprintln("5是否大于 4：\(5 > 4)")// 输出falseprintln("3的4次方是否大于等于90.0：\(pow(3, 4) >= 90.0)")// Swift要求比较运算符两边的数值的类型完全一样，// 因此必须把前面Int类型强转为Double之后再比较，输出trueprintln("20是否大于等于20.0：\(Double(20) >= 20.0)")// 输出trueprintln("5和5.0是否相等：\(Double(5) == 5.0)")// 输出falseprintln("true和false是否相等：\(true == false)")// 创建2个对象var d1 = NSMutableArray()var d2 = NSMutableArray()// 判断两个对象的内容是否相等，输出trueprintln(d1 == d2)// 判断d1、d2是否指向同一个对象，输出falseprintln(d1 === d2)