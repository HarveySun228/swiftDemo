/* 定义两个类型参数，其中：SrcType代表需要执行投影的数组的元素类型。DescType代表投影得到的数组的元素类型。*/func projection <SrcType , DescType> (src:[SrcType] 	, fn:(SrcType) -> DescType) -> [DescType]{	// 创建空数组	var result = [DescType]()	// 遍历源数组	for element in src	{		// 使用fn函数对数组元素执行投影计算，将计算结果添加到result数组中		result.append(fn(element))	}	return result}var books = ["疯狂Swift讲义"	, "疯狂iOS讲义" , "疯狂Android讲义" , "疯狂Java讲义"]// 使用尾随闭包var proj1 = projection(books){	// 闭包决定投影逻辑，就是统计每个数组元素的长度	countElements($0)}  // ①println(proj1)  // 输出 [9, 7, 11, 8]var proj2 = projection(books){	"《" + $0 + "》"}println(proj2) // 输出 [《疯狂Swift讲义》, 《疯狂iOS讲义》, 《疯狂Android讲义》, 《疯狂Java讲义》]// 使用尾随闭包var proj3 = projection(books){	(b: String) -> (String , String) in	return (b , "李刚")}println(proj3)