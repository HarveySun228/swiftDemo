import Foundation// 定义一个可被Objective-C类使用的协议@objc protocol TestProtocol{}// 声明hello时使用NSObject类，则hello的编译类型是NSObejct，// hello变量的实际类型是NSStringlet hello:NSObject = "Hello"println("字符串是否是NSString类的实例：\(hello is NSString)")  // 返回true。// NSDate与NSObject类存在继承关系，可以进行is运算。println("字符串是否是NSDate类的实例：\(hello is NSDate)")  //返回false。// NSString没有实现TestProtocol协议，所以返回false。println("字符串是否是TestProtocol协议的实例：\(hello is TestProtocol)")  //返回false。let a: NSString = "Hello"// NSString类与NSDate类没有继承关系，所以下面代码编译出错//println("字符串是否是Math类的实例：\(a is NSDate)")