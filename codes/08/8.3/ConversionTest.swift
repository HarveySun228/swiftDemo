import Foundationlet obj: NSObject = "Hello"// obj变量的编译类型为NSObject，NSObject与NSString存在继承关系，可以进行转换// 而且obj实际引用的实例是NSString类型，所以运行时也可通过let objStr:NSString = obj as NSStringprintln(objStr)// 定义一个objPri变量，编译类型为NSObject，实际类型为NSNumberlet objPri:NSObject = 5// objPri变量的编译时类型为NSObject，objPri的运行时类型为NSNumber// NSObject与NSString存在继承关系，可以进行转换，编译通过// 但objPri变量实际引用的实例是NSNumber类型，所以转换失败let str:NSString = objPri as NSString  // 转化失败let s: NSString = "Swift"// s变量的编译时类型为NSString，s的运行时类型为NSString// 但NSString与NSNumber不存在继承关系，因此不可进行转化，编译报错//let it:NSNumber = s as NSNumber  // 编译报错