import Foundation// 定义一个String字符串var str1 = "学习Swift很有趣，大家要好好学习！"// 直接使用String的方法，将字符串内容写入指定文件str1.writeToFile("myFile.txt", atomically:true ,	encoding:NSUTF8StringEncoding, error:nil)// 读取文件内容，用文件内容初始化字符串var str2 = NSString(contentsOfFile: "myFile.txt",	encoding: NSUTF8StringEncoding, error: nil)println(str2)var str:NSString = "Hello"let book = "《疯狂Swift讲义》"// 在str后面追加固定的字符串// 原来的字符串对象并不改变，只是将新生成的字符串重新赋给str指针变量str = str.stringByAppendingString(",iOS!")str = str.stringByAppendingString(book)println(str)println("str的字符个数为：\(str.length)")// 获取str的前几个字符组成的字符串let n = 7let s1 = str.substringToIndex(n)println("str的前\(n)个字符为\(s1)")// 获取str从第10个字符开始、与后面所有字符组成的字符串let s2 = str.substringFromIndex(10)println("从索引10开始的子串为：\(s2)")// 获取str从第5个字符开始，长度为7的字符组成的字符串let s3 = str.substringWithRange(NSMakeRange(5, 7))println(s3)// 获取iOS在str中出现的位置var pos = str.rangeOfString("iOS")println("iOS在str中出现的开始位置：\(pos.location), 长度为：\(pos.length)")// 将str的所有字符转为大写str = str.uppercaseStringprintln(str)